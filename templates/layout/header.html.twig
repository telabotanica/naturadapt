<header class="main-header">
	<div class="main-header--logo">
		<a href="{{ path('homepage') }}">{{ plateform.name }}</a>
	</div>
	<div class="main-header--links">
		<div class="main-header--pages">
			<ul>
				<li><a href="{{ path('groups_index') }}">Groupes</a></li>
				<li><a href="">À propos de la plateforme</a></li>
			</ul>
		</div>
		<div class="main-header--user">
			{% if not is_granted('IS_AUTHENTICATED_REMEMBERED') %}
				<a class="connexion" href="{{ path('user_login') }}">{{ 'pages.user.login.link'|trans }}</a>
			{% else %}
				{% include 'contents/user/user--tiny.html.twig' with {'user':app.user} %}
				<div class="links">
					<ul>
						<li><a href="{{ path('user_profile_edit') }}">{{ 'pages.user.profile_edit.link'|trans }}</a></li>
						<li><a href="{{ path('user_logout') }}">{{ 'pages.user.logout.link'|trans }}</a></li>
					</ul>
				</div>
			{% endif %}
		</div>
	</div>
</header>
