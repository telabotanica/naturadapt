{% extends 'base.html.twig' %}

{% block main %}
	<section class="main main__group-page main__group-page__edit">
		<div class="main--content content-container">
			{% include 'components/app-flashes.html.twig' %}

			<section class="group-header">
				<h1 class="h1">{{ 'pages.group.documents.title'|trans }}</h1>

				{% include 'components/breadcrumb.html.twig' with {'items':[
					{
						label: 'pages.group.groups.title'|trans,
						href: path('groups_index')
					},
					{
						label: group.name,
						href: path('group_index', {'groupSlug':group.slug})
					},
					{
						label: 'pages.group.documents.title'|trans,
						href: path('group_documents_index', {'groupSlug':group.slug})
					},
					{
						label: 'pages.document.document_create.title'|trans
					}
				]} %}
			</section>

			<section class="group-form">
				{% include 'forms/document/document-form.html.twig' with ({
					'form':form,
					'form_submit':'pages.document.document_create.button',
				}) %}
			</section>
		</div>
	</section>
{% endblock %}
